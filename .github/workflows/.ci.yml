name: Run Hudl UI Tests Securely

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  ui-tests:
    runs-on: ubuntu-latest  # Use Ubuntu for stability (pre-installed Chrome & Firefox)

    env:
      BROWSER: chrome  # Default browser to avoid missing env variable issues

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run UI Tests
        run: |
          mkdir -p reports  # Ensure the reports directory exists
          pytest hudl/test/ -m login_test --html=reports/report.html --self-contained-html --title="Hudl UI Tests"

      - name: Upload Test Report
        uses: actions/upload-artifact@v3
        with:
          name: test-report
          path: reports/report.html
